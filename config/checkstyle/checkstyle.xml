<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Allows packages without a package-info.java file -->
    <module name="JavadocPackage">
        <property name="allowMissingPackageInfo" value="true"/>
    </module>

    <module name="TreeWalker">

        <!-- Javadoc checks removed entirely to avoid enforcement -->

        <!-- Allow opening curly brace on the same line -->
        <module name="LeftCurly">
            <property name="option" value="eol"/>
            <property name="ignoreEnums" value="true"/>
        </module>

        <!-- Do not enforce specific closing brace placement -->
        <!-- RightCurly module omitted -->

        <!-- Helpful lightweight rules -->
        <module name="UnusedImports"/>
        <module name="AvoidStarImport"/>
        <module name="FinalLocalVariable"/>
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>

    </module>
</module>
